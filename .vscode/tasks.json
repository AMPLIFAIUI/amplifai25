{
	"version": "2.0.0",
	"tasks": [
		{
			"type": "shell",
			"label": "Check Python dependencies and missing modules",
			"command": "pip check && pip list && pip freeze",
			"isBackground": false
		}
	]
}